# assets/

Imported static assets (SVGs, illustrations, small images bundled by Vite).
Larger public files (resume PDF, project screenshots, OG image) live in
`/public` — see `docs/TODO.md` for what's still needed.
